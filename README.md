# Simple NPM static frontend environment

This is a lightweight frontend development environment.  
It enables the following:

- Live browser sync
- SCSS compilation
- JS aggregation and minification
- Images minification

## Available commands

`npm run watch`  
`npm run build`  
`npm run clean`  

